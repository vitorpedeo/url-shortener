<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
        integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous">
    </script>
</head>

<body>

    <div class="container text-center">
        <h1 class="mt-5">URL Shortener</h1>
        <form action="/shrinker" method="post">
            <input placeholder="Enter the URL" type="text" name="url" class="form-control mt-5 mb-5" required>
            <button type="submit" class="btn btn-primary btn-lg btn-block">Shrink It!</button>
        </form>

         <p class="mt-5"><strong>Attention!</strong></p>
         <p>The shortened URL will only work in this page!</p>
         <p>That means you can't copy/paste the URL to your navigation bar!</p>

        <div class="table-responsive">
            <table class="table table-striped mt-5">
                <thead class="thead-dark">
                    <tr>
                        <th>Original URL</th>
                        <th>Shortened URL</th>
                    </tr>
                </thead>

                <tbody>

                    <% URLs.forEach(url => { %>
                    <tr>
                        <td> <a href=" <%= url.original %> "> <%= url.original %> </a></td>
                        <td> <a href="<%= url.original %>"><%= url.shortened %></a> </td>
                    </tr>
                    <% }); %>

                </tbody>
            </table>
        </div>
    </div>

</body>

</html>